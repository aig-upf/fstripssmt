(define (problem cells_4)
(:domain simple-grid-fn)
(:objects
)
(:init
    (= (max_x) 1)
    (= (max_y) 1)
    (= (x) 0)
    (= (y) 0)
)

(:goal
    (and
        (= (x ) 1)
        (= (y ) 1)
    )
)
)
